function initLogin() {
    // startAnimation();
    // check localStorage if user is logged in, if yes redirect to summary, if no show login template
}

//! Form Validation, wenn was nicht ok ist, .error class hinzufügen